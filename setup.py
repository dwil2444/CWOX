from setuptools import setup, find_packages


setup(
    name='CWOX',
    version='0.1',
    packages=find_packages(where='u/dw3zn/GitHub_Repos/CWOX'),  # Use find_packages to include all packages
    install_requires=[
        # Add your dependencies here
    ],
    # Other setup configurations...
)